# Copy to `.env` (git-ignored) and fill in the keys you actually use.
# Never commit `.env`, and never paste a key into a chat, an issue, or a commit.
# Only the providers listed in models.yaml are needed; delete the rest.

OPENAI_API_KEY=
# ANTHROPIC_API_KEY=
# GEMINI_API_KEY=
# DEEPSEEK_API_KEY=
# DASHSCOPE_API_KEY=

# Optional: route OpenAI-SDK calls to a compatible endpoint (translate.py).
# OPENAI_BASE_URL=
